# ============================================================
# Aceleração por GPU NVIDIA (CUDA 12) - OPCIONAL
# ============================================================
# Só faz sentido se você tem uma GPU NVIDIA com driver recente.
# Download grande (~1,3 GB). SEM estes pacotes o app usa a CPU
# automaticamente (fallback embutido).
#
# Instalação (depois do requirements.txt):
#     pip install -r requirements-gpu.txt
# ------------------------------------------------------------
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.24.0.43
nvidia-cuda-runtime-cu12==12.9.79
